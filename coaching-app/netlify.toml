[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[functions]
  external_node_modules = ["*"]
  node_bundler = "esbuild"

[functions."claude"]
  timeout = 26

[[redirects]]
  from = "/api/claude"
  to = "/.netlify/functions/claude"
  status = 200
